# Frontend environment (safe-to-publish values — the anon key is public by design).
# For local dev: copy to web/.env. On Vercel: set these as project env vars.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
